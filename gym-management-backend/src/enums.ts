export enum Role {
  Admin = "admin",
  Manager = "manager",
  Member = "member",
}

export enum MembershipStatus {
  Active = "active",
  Expired = "expired",
  Cancelled = "cancelled",
  Pending = "pending",
}

export enum Tier {
  WalkIn = "walk-in",
  Silver = "silver",
  Gold = "gold",
}

export enum PlanDuration {
  Monthly = "monthly",
  SemiAnnual = "semi-annual",
  Annual = "annual",
}
